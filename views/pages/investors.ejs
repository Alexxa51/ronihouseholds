<% const styles = typeof customStyles !== 'undefined' ? customStyles : ['/css/main.css', '/css/financial.css', '/css/investors.css']; %>
<%- include('../partials/header', { customStyles: styles }) %>
<section class="hero">
  <h1>Disclosures under Regulation 46 of LODR</h1>
</section>

<% Object.keys(grouped).forEach(page => { 
     const sectionTitle = page.replace(/-/g, ' ').toUpperCase(); 
%>
  <%- include('../partials/document-section', { sectionTitle, files: grouped[page] }) %>
<% }) %>


<%- include('../partials/footer') %>